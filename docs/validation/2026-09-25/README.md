# 真实模型验收：todo-status-v1

日期：2026-09-25（Asia/Shanghai）。原始记录的时间字段使用 UTC，起止为 2026-09-24 19:14:33–19:27:29 UTC。环境：Windows、Python 3.12.7、Codex CLI 0.155.0-alpha.16.3；沿用本机已有模型配置。

本次使用真实 `CodexAdapter`，没有使用离线 `ScriptedAdapter`。最终状态为 `completed`，实现尝试一次。内置起始样例仍保持未实现状态筛选，以供下一次红灯演示；生成的最终实现与测试保存在本次运行工作副本及下方差异文件中。

| 验证 | 实际结果 |
| --- | --- |
| 初始化基线 | 3 项通过 |
| 新增特性测试的红灯 | 14 项测试，12 次断言失败（含子测试），0 errors、0 skipped |
| 红灯时原有基线 | 3 项通过 |
| 保存红灯后使用新进程恢复 | 成功 |
| 绿灯回归 | 17 项全部通过 |
| 独立验收 | 5 项全部通过 |
| 实现尝试 | 1 次，未使用修复轮次 |
| 项目自身回归测试 | 18 项全部通过 |

## 过程与发现

共发起 4 次真实角色调用：两次 planner、一次 test_writer、一次 executor。第一次规划进程成功退出并返回完整结果，但适配器把先前 `Reconnecting... N/M (...)` 网络重连通知当成终止错误。修复前新增的回归测试复现了相同误判；修复后，只有已知重连通知之后出现完成事件、退出码为零且结果合法时才接受。其他错误仍被拒绝，恢复通知保存在诊断 `warnings` 中。

修复后用 `resume` 重新调用 planner，没有把第一次结果手动写入状态。规格与计划经监督助手核对后，在用户“继续”授权范围内执行 `approve`，不是用户另行逐条审阅生成计划。随后执行 `resume --stop-after red`，检查真实断言失败，再用新进程 `resume` 完成实现和独立验收。

这是应用阶段检查点恢复的验证，不是断电或任意文件损坏恢复测试。一次固定示例成功也不能证明通用仓库成功率、恶意代码隔离或稳定的性能指标。

## 可复核证据

- [结构化验证记录](verification.json)：阶段、时间、审批摘要、文件摘要、四次调用的完成事件与诊断。第一次调用被适配器拒绝的事实保留在其中。
- [生成的规格](spec.json)与[计划](plan.md)。
- [红灯日志](red.log)：真实 AssertionError，绝对运行路径替换为 `<run>`。
- [绿灯日志](green.log)与[独立验收日志](acceptance.log)。
- [生成的实现与测试差异](diff.patch)：仅新增特性测试及修改 `todo_api.py`。

`verification.json` 的调用完成事件包含 CLI 实际返回用量，包括首次被适配器拒绝的调用；这些字段不是费用账单。原始事件流和 stderr 留在本地运行目录，公开证据未包含账户配置或凭据。

本地完整报告由 `python -m specflow report work/live-todo-20260925` 生成。该运行目录被 Git 忽略；以上归档证据随源码提交。
